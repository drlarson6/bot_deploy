# --- Required at runtime ---
OPENAI_API_KEY=         # your OpenAI key, set in Cloud Run
PORT=8080               # Cloud Run injects $PORT automatically

# --- Optional ---
DF_WEBHOOK_TOKEN=       # optional: verify DF webhook requests

# --- Informational only ---
GOOGLE_PROJECT_ID=zelora-prod
